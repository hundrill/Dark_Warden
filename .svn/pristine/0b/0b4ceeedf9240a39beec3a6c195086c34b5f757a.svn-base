using UnityEngine;

public class ProfileOnOff : MonoBehaviour
{
	// Start is called once before the first execution of Update after the MonoBehaviour is created
	void Start()
	{
		if (UI_Main.instance)
			UI_Main.instance.OnMainUiChange += OnMainUiChange;
	}

	void OnMainUiChange(MAINUI _mainui)
	{
		if (_mainui == MAINUI.PROFILE)
		{

		}
	}
}
