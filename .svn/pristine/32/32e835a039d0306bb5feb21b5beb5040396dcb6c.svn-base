using TMPro;
using UnityEngine;
using static SkillManager;

public class Reward_Ui : MonoBehaviour {

    public delegate void onReward_Select(int idx_preset);
    public onReward_Select OnReward_Select;

    private void OnEnable()
    {
        Setting();
    }

    void Setting()
    {

        /*if (SkillManager.instance)
        {
            SkillManager.instance.OnSkillUse += OnSkillUse;
            SkillManager.instance.OnCoolTime_Remain += OnCoolTime_Remain;
            OnSkillReady += SkillManager.instance.OnSkillReady;
            OnSkillClose += SkillManager.instance.OnSkillClose;

            //StartCoroutine(CoolTime());
            break;
        }*/

    }

    public void SelectReward()
    {

    }
}
